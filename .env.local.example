# Example local URLs when using root `docker-compose.yml` (Postgres + Redis).
# Copy to `server/.env` and fill remaining keys from your team’s checklist (JWT, SendGrid, etc.).

DATABASE_URL="postgresql://vector:vector@localhost:5432/vector"
REDIS_URL="redis://localhost:6379"
